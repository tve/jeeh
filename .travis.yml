# Continuous Integration (CI) is the practice, in software
# engineering, of merging all developer working copies with a shared mainline
# several times a day < http://docs.platformio.org/page/ci/index.html >
#
# Documentation:
#
# * Travis CI Embedded Builds with PlatformIO
#   < https://docs.travis-ci.com/user/integration/platformio/ >
#
# * PlatformIO integration with Travis CI
#   < http://docs.platformio.org/page/ci/travis.html >
#
# * User Guide for `platformio ci` command
#   < http://docs.platformio.org/page/userguide/cmd_ci.html >

language: python
python:
    - "2.7"

sudo: false
cache:
    directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=examples/blink.cpp
    - PLATFORMIO_CI_SRC=examples/hello.cpp
#   - PLATFORMIO_CI_SRC=examples/printf.cpp
#   - PLATFORMIO_CI_SRC=examples/i2c.cpp
#   - PLATFORMIO_CI_SRC=examples/spiflash.cpp

install:
    - pip install -U platformio
    - platformio update

script:
    - platformio ci --lib="." -C examples/platformio.ini
