script [find board/st_nucleo_g4.cfg]

adapter srst delay 0 ;# get rid of a small-but-superfluous 100 ms delay

switch [info hostname] {
    jcwair* { adapter serial 0024002E3137510739383538 }
    nukkie  { adapter serial 002900393532510F31333430 }
}

#set fcpu 16000000
#set fswo 8000000
set fcpu 150000000
set fswo 9375000

$_CHIPNAME.tpiu configure \
                -protocol uart -traceclk $fcpu -pin-freq $fswo -output :6464
$_CHIPNAME.tpiu enable
itm ports on

$_TARGETNAME configure -event reset-start {
	adapter speed 1000 ;# avoid the default 2000 kHz warning
}

$_TARGETNAME configure -event trace-config {
    poll off ;# no need to poll for programming and ITM / SWO use
}
