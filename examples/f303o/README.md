### Example code for an Olimexino F303RC board.

DCF77 module connections: dat = PA4 (w/ pull-up), pon = PB7 (inverted).

MSF60 module connections: dat = PC2 (w/ pull-up), pon = PC1 (both inverted).

GPS module connections: gps-rx = PA9, gps-tx = PA10 (i.e. Arduino D7 & D8).
